import React from 'react'
import './Errorpage.css'

const ErrorTitle = (props) => {
    return (
        <div id='error-title'>
            {props.title}
        </div>
    )
}

export default ErrorTitle
